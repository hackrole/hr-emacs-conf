;; Standard key bindings, 
;; TO SEE the hr-keys-conf

;; set org-agenda
(setq org-agenda-files '("~/.org/learn.org"
						 "~/.org/read.org"
						 "~/.org/life.org"))

;; set keys
;; (define-key )

;; provide
(provide 'hr-org-conf)
