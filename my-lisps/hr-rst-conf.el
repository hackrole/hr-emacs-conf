;; emacs conf for rst-mode
;; if emacs-version <= 24.3 need another download install
(add-to-list 'load-path "~/.emacs.d/plugins/rst")
(require 'rst)

;; TODO the require error
;; (load "~/.emacs.d/plugins/rst/rst.el")

;; provide
(provide 'hr-rst-conf)
